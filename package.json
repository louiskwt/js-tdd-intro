{
  "name": "test-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "jest --watchAll --verbose"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "cypress": "^8.5.0",
    "vite": "^2.6.0"
  },
  "dependencies": {
    "jest": "^27.2.4"
  }
}
